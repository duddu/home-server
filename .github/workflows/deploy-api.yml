name: Deploy API
on:
  push:
    branches:
      - main
    paths:
      - 'packages/rust-api/**'
jobs:
  trigger-docker-image-ci:
    uses: ./.github/workflows/docker-image.yml
    with:
      package: rust-api
      image_name: rust-home-server-api
