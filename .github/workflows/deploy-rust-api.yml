name: Deploy API
on:
  push:
    branches:
      - main
    paths:
      - 'packages/rust-api/**'
jobs:
  trigger-image-ci:
    name: Trigger image CI
    uses: ./.github/workflows/docker-image.yml
    secrets: inherit
    with:
      package: rust-api