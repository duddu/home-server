apiVersion: batch/v1
kind: CronJob
metadata:
  labels:
    job: home-server-google-ddns
  name: home-server-google-ddns
  namespace: home-server
spec:
  schedule: "*/20 * * * *"
  concurrencyPolicy: Replace
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            job: home-server-google-ddns
        spec:
          containers:
            - image: ghcr.io/duddu/home-server-google-ddns:0.3.0
              imagePullPolicy: Always
              name: google-ddns
              env:
                - name: GOOGLE_DDNS_USERNAME
                  valueFrom:
                    secretKeyRef:
                      name: google-ddns-credentials
                      key: username
                      optional: false
                - name: GOOGLE_DDNS_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: google-ddns-credentials
                      key: password
                      optional: false
              volumeMounts:
                - mountPath: /var/public-ip
                  name: nfs-public-ip-pvc
          restartPolicy: OnFailure
          volumes:
            - name: nfs-public-ip-pvc
              persistentVolumeClaim:
                claimName: nfs-public-ip